{
  "name": "melodify",
  "version": "1.0.0",
  "description": "Melodify music streaming platform — monorepo root",
  "scripts": {
    "postinstall": "cd backend && npm install && npx prisma generate",
    "start": "cd backend && node src/server.js",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18"
  }
}
